"""Composite multi-signal scanner (Phase D3).

The synthesis edge: no single free signal is strong, but a *transparent* blend of
technical momentum + smart-money flows + edge signals, ranked across the universe
and gated by market regime, surfaces the names where several weak tailwinds line up.
The scoring is pure + testable; every score shows its component breakdown so it's
never a black box. Honest: the blend is a heuristic, not a validated alpha.
"""
from __future__ import annotations

from trading_llm.scanner.composite import score_symbol, WEIGHTS

__all__ = ["score_symbol", "WEIGHTS"]
